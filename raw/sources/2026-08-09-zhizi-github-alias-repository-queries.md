# GitHub API：Autokernel、KernelCAT、Kerminal 名称检索

> Sources:
> - https://api.github.com/search/repositories?q=autokernel
> - https://api.github.com/search/repositories?q=kernelcat
> - https://api.github.com/search/repositories?q=kerminal
> Collected: 2026-08-09
> Published: Unknown

## 原始 JSON 响应中的检索结果摘要

`autokernel` 的 `total_count` 为 40，靠前结果包括 `RightNow-AI/autokernel`、`OAID/AutoKernel`、`oddlama/autokernel`、`OAID/autokernel-docs-cn`。

`kernelcat` 的 `total_count` 为 1，结果为 `0xHadiRamdhani/kernelcate-documentation`。

`kerminal` 的 `total_count` 为 12，靠前结果包括 `klpod221/kerminal`、`kongweiguang/kerminal`、`SavinaRoja/Kerminal`、`kerminal/kerminal`。

上述公开仓库名称和组织名未显示“智子芯元”或“Autokernel Co., Ltd.”归属信息。名称检索只能说明同名噪声存在，不能认定这些仓库与智子芯元有关或无关，也不能证明该公司是否拥有私有仓库。
